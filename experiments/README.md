# Experiments

This directory contains exploratory model-selection/training scripts used during analysis.

- `kc_housing.py`
- `model_selection.py`

These are **not** part of the deployed API serving path (`app/` + `scripts/`).

Run from repo root if needed:

```bash
python experiments/model_selection.py
python experiments/kc_housing.py
```
